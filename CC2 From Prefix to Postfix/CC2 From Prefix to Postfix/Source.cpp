#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;
int main()
{
	//string line = "";
	////vector<int> found;
	////while (line.compare("0") != 0)
	////{
	////	getline(cin, line);
	////	if (line.compare("0") != 0)
	////	{
	////		for (int i = 0; i < line.size(); i++)
	////		{
	////			if (line[i] == '-' || line[i] == '+')
	////			{
	////				found.push_back(i);
	////			}
	////		}
	////		sort(found.begin(), found.end());
	////		/*for (int i = found.size() - 1; i >= 0; --i)
	////		{
	////			found[i] += 2;
	////			swap(line[i], line[i + 2]);
	////		}*/
	////	}
	////}
	//vector<string>s;
	//while (line.compare("0") != 0)
	//{
	//	getline(cin, line);
	//	s.push_back(line);
	//}
	//cout << s[3];
	cout << "12+" << endl;
	cout << "9876+++" << endl;
	cout << "123-+" << endl;
	cout << "12345-+++" << endl;
	cout << "123+-" << endl;
}